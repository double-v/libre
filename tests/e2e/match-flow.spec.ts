import { test, expect } from '@playwright/test';

test.describe('Match flow', () => {
  test('user can like someone from crossings', async ({ page }) => {
    // Requires authenticated state + test fixtures
    test.skip();
  });
});